{
    "scripts": {
        "start": "cd gui && npm run start",
        "win:build": "cd gui && npm run win:build",
        "mac:build": "cd gui && npm run mac:build",
        "cli": "cd cli && node index"
    }
}
