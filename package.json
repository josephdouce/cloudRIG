{
    "scripts": {
        "start": "cd gui && npm start",
        "build": "cd gui && npm build",
        "release": "cd gui && npm release",
        "gui": "npm start",
        "cli": "cd cli && node index"
    }
}
