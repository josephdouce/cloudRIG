{
	"name": "cloudrig-gui-builder",
	"version": "0.0.8",
	"license": "MIT",
	"scripts": {
		"postinstall": "concurrently \"cd react && npm install\" \"cd electron && npm install\" ",
		"start": "concurrently \"cd react && npm start\" \"cd electron && npm start\"",
		"build:electron": "cd electron && npm install",
		"build:react": "cd react && npm run-script build",
		"build:prepare": "concurrently \"npm run-script build:electron\" \"npm run-script build:react\"",
		"build": "npm run-script build:prepare && cd electron && electron-builder"
	},
	"devDependencies": {
		"concurrently": "^4.1.0"
	},
	"dependencies": {
		"electron-builder": "^20.38.5"
	}
}
